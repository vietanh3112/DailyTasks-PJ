<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/light_gray" /> <!-- Màu nền -->
    <stroke
        android:width="1dp"
        android:color="@color/white" /> <!-- Màu và độ dày viền -->
    <corners android:radius="8dp" /> <!-- Chỉnh radius ở đây, ví dụ 8dp -->
</shape>
